spring:
  datasource:
    url: 'jdbc:postgresql://localhost:15432/postgres'
    username: 'postgres'
    password: 'admin'
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
#        show_sql: true
logging:
  level:
    root: info
    sql: debug
    'io.d4e': debug
